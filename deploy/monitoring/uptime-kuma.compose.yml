services:
  uptime-kuma:
    image: louislam/uptime-kuma:2
    container_name: uptime-kuma
    restart: unless-stopped
    ports:
      - "${UPTIME_KUMA_BIND:-127.0.0.1}:${UPTIME_KUMA_PORT:-3001}:3001"
    volumes:
      - "${UPTIME_KUMA_DATA_DIR:-/opt/uptime-kuma/data}:/app/data"

