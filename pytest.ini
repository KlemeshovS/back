[pytest]
testpaths = backend/tests
pythonpath = backend
markers =
    integration_db: tests that require TEST_DATABASE_URL and a real PostgreSQL instance
filterwarnings =
    ignore::pydantic.warnings.UnsupportedFieldAttributeWarning
